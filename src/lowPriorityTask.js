import getRandomPlantSvg from "./randomPlant.js";
// const lowPriortiyChoice = document.getElementById("priorityChoice");
// const plantArea = document.getElementById("plantArea");

// Sets the innerHTML of the container to the SVG string
// plantContainer.innerHTML = randomSvgString;

// Adds the new plant container to the page
// plantArea.appendChild(plantContainer);
